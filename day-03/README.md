**Why are we NOT using an LLM for intent classification?

- We should not use LLM for intent clasification beacuse we will have free floating text and if we dependent on Llm then it would add unncecessay cost to the model. we can do it by if else logic and manual rules and use llm only for reasoning 

